import React from 'react';

import './GorganyButton.css'

const GorganyButton = ({title}) => {
    return (
        <button className="gorgany-button">{title}</button>
    );
};

export default GorganyButton;